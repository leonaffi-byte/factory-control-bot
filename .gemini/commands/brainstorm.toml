description = "Multi-model brainstorm via zen MCP"
prompt = "Present topic to 3 different models via zen MCP (never same provider). Synthesize into recommendation with trade-offs. Save to artifacts/architecture/brainstorm.md."
